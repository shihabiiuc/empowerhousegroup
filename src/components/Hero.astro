<section class="hero">
  <div class="badge">Behavioral Fitness Coaching</div>
  <h1>
    The World is Ready for Empowered People<br />
    <span class="accent">Starting with You.</span>
  </h1>
  <p class="hero-sub">
    The BioCode System is a practical, actionable discipline building relentless
    inner-strength and optimism. And it's about to make you unshakable.
  </p>
  <div class="hero-actions">
    <a href="/self-assessment" class="btn-primary">Get Your Free Score →</a>
    <a href="/the-biocode-system" class="btn-ghost">Learn the System</a>
  </div>
</section>

<style>
  /* ── Hero ── */
  .hero {
    text-align: center;
    padding: 7rem 1rem 5rem;
    margin: 0 auto;
    animation: fadeUp 0.7s ease both;
    background-color: var(--bg-primary);
  }

  .hero h1 {
    font-family: "Cormorant Garamond", serif;
    /* font-size: clamp(2.4rem, 7vw, 4rem); */
    /* font-size: clamp(3.6rem, 8vw, 6.5rem); */
    font-weight: 400;
    line-height: 1.08;
    color: var(--text-muted-light);
    margin-bottom: 1.25rem;
  }

  .accent {
    color: var(--accent);
  }

  .hero-sub {
    font-family: var(--font-body);
    font-size: 1.05rem;
    color: var(--text-muted);
    line-height: 1.75;
    max-width: 580px;
    margin: 0 auto 2.5rem;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
