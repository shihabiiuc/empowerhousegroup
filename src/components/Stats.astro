<section class="stats">
  <div class="stat">
    <strong>2,400+</strong>
    <span>Clients coached</span>
  </div>
  <div class="stat-divider"></div>
  <div class="stat">
    <strong>94%</strong>
    <span>Report lasting change</span>
  </div>
  <div class="stat-divider"></div>
  <div class="stat">
    <strong>12 weeks</strong>
    <span>Average transformation</span>
  </div>
  <div class="stat-divider"></div>
  <div class="stat">
    <strong>5-star</strong>
    <span>Average client rating</span>
  </div>
</section>

<style>
  .stats {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 2rem 3rem;
    margin: 0 auto 6rem;
    max-width: 900px;
    animation: fadeUp 0.7s 0.15s ease both;
  }

  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    padding: 0.5rem 2.5rem;
  }

  .stat strong {
    font-family: var(--font-heading);
    font-size: 1.9rem;
    color: var(--accent);
  }

  .stat span {
    font-family: var(--font-body);
    font-size: 0.8rem;
    color: var(--text-muted);
    white-space: nowrap;
  }

  .stat-divider {
    width: 1px;
    height: 40px;
    background: var(--border);
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
